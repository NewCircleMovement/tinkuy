= simple_form_for [:admin, @resource], :html => {:role => 'form' } do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :duration, :label => "Angiv tidsinterval for 'timeslots', fx. 2 "

  .form-actions
    = f.button :submit
