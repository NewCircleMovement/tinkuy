= simple_form_for [:admin, @center], :html => {:role => 'form' } do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :max_members

  .form-actions
    = f.button :submit
