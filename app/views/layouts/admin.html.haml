!!!
%html
  %head
    %title Tinkuy
    = stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags
  %body
    %nav.navbar.navbar-inverse.navbar-default{:role => "navigation"}
      .container-fluid
        / Brand and toggle get grouped for better mobile display
        .navbar-header
          %button.navbar-toggle.collapsed{"data-target" => "#bs-example-navbar-collapse-1", "data-toggle" => "collapse", :type => "button"}
            %span.sr-only Toggle navigation
            %span.icon-bar
            %span.icon-bar
            %span.icon-bar
          %a.navbar-brand{:href => root_url} ADMIN
        / Collect the nav links, forms, and other content for toggling
        #bs-example-navbar-collapse-1.collapse.navbar-collapse
          %ul.nav.navbar-nav
            %li{:class => ( controller_name == 'events' ? 'active' : '' )}
              = link_to "Aktiviteter", admin_events_path
            %li{:class => ( controller_name == 'users' ? 'active' : '' )}
              = link_to "Medlemmer", admin_users_path
            %li{:class => ( controller_name == 'resources' ? 'active' : '' )}
              = link_to "Resourcer", admin_resources_path
          
    .container
      - flash.each do |name, msg|
        .alert.alert-success
          = content_tag :div, msg, id: "flash_#{name}"
      = yield
